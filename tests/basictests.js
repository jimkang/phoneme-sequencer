var assert = require('assert');
var phonemeSequencer = require('../phoneme-sequencer');

describe('The basic stuff', function basicSuite() {
  it('should return a follower',
    function testThing(testDone) {
      testDone();
    }
  );
});
